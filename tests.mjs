import assert from 'node:assert/strict'; import { normalizeTicket, calculateMetrics, filterByChannel, tickets } from './app.mjs'; assert.deepEqual(normalizeTicket({score:'4',resolved:1}), {score:4,resolved:true}); assert.equal(calculateMetrics(tickets).resolved, 3); assert.equal(filterByChannel(tickets,'Chat').length, 2); console.log('customer-insights-dashboard-demo: testes aprovados');